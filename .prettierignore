# Prettier governs Markdown only in this repository; oxfmt owns every other
# format. `hub format` always hands Prettier an explicit `**/*.md` target set,
# so this file only has to carve out the paths within that set to leave alone.

# Build output and caches.
**/node_modules/
**/dist/
**/coverage/
**/test-results/
**/.wrangler/

# Third-party Markdown, kept byte-for-byte as received from upstream.
packages/icons/data/
packages/skills/skills/
