root = true

[*]
indent_style = space
indent_size = 2
insert_final_newline = true
trim_trailing_whitespace = true

[*.md]
trim_trailing_whitespace = false
# Prose is not hard-wrapped; `pnpm format` runs Prettier with proseWrap "never".
max_line_length = off
