{
  "name": "@codenhub/router",
  "version": "0.0.2",
  "private": false,
  "description": "Small browser router for TypeScript apps.",
  "homepage": "https://github.com/codenhub/codenhub/tree/main/packages/router",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/codenhub/codenhub.git",
    "directory": "packages/router"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsdown src/index.ts --format esm --dts --clean --no-fixed-extension",
    "dev": "pnpm --filter=@codenhub/router-dev dev",
    "debug": "pnpm build && pnpm --filter=@codenhub/router-debug dev",
    "prepublishOnly": "pnpm build && pnpm typecheck",
    "status:npm": "npm view @codenhub/router version dist-tags time --json && npm dist-tag ls @codenhub/router && npm access get status @codenhub/router",
    "status:pack": "npm pack --dry-run",
    "test": "vitest run && pnpm build && pnpm test:visual",
    "test:coverage": "vitest run --coverage",
    "test:visual": "playwright test",
    "test:watch": "vitest",
    "test:visual:watch": "playwright test --ui",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "jsdom": "catalog:",
    "tsdown": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
